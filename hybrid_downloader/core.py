"""\nCore Hybrid Downloader implementation.\n\nMain orchestrator for downloading music from Spotify URLs/queries.\nCoordinates Spotify API, YouTube search, audio download, and metadata tagging.\n\nLicense: MIT\n\"\"\"\n\nimport logging\nfrom pathlib import Path\nfrom typing import List, Optional\n\nfrom .spotify_api import SpotifyAPI, Track\nfrom .downloader import AudioDownloader\nfrom .metadata import MetadataHandler\nfrom .paths import PathManager\nfrom .exceptions import (\n    SpotifyCredentialsError,\n    DownloadError,\n    InvalidURLError,\n    FFmpegNotInstalledError,\n)\nfrom .types import Type\nfrom .utils import is_valid_spotify_url\n\nlogger = logging.getLogger(__name__)\n\n\nclass HybridDownloader:\n    \"\"\"Main downloader class combining Spotify metadata and YouTube audio.\"\"\"\n    \n    def __init__(self, client_id: Optional[str] = None,\n                 client_secret: Optional[str] = None,\n                 download_path: Optional[str] = None,\n                 audio_format: str = 'mp3',\n                 add_cover_art: bool = True,\n                 max_retries: int = 3):\n        \"\"\"Initialize Hybrid Downloader.\n        \n        Args:\n            client_id: Spotify Client ID (or use SPOTIPY_CLIENT_ID env var)\n            client_secret: Spotify Client Secret (or use SPOTIPY_CLIENT_SECRET env var)\n            download_path: Directory to save downloads (defaults to ~/Music/HybridDownloader)\n            audio_format: Audio format (mp3, flac, aac, wav, opus)\n            add_cover_art: Whether to add cover art to files\n            max_retries: Number of download retry attempts\n            \n        Raises:\n            SpotifyCredentialsError: If Spotify credentials not available\n            FFmpegNotInstalledError: If FFmpeg not installed\n        \"\"\"\n        # Initialize path manager\n        self.path_manager = PathManager(download_path=download_path)\n        \n        # Setup logging\n        self._setup_logging()\n        \n        # Initialize Spotify API\n        try:\n            self.spotify = SpotifyAPI(client_id, client_secret)\n        except SpotifyCredentialsError as e:\n            logger.error(str(e))\n            raise\n        \n        # Initialize audio downloader\n        try:\n            self.downloader = AudioDownloader(\n                self.path_manager.get_download_dir(),\n                audio_format=audio_format,\n                max_retries=max_retries\n            )\n        except FFmpegNotInstalledError as e:\n            logger.error(str(e))\n            raise\n        \n        self.audio_format = audio_format\n        self.add_cover_art = add_cover_art\n        self.stats = {\n            'total': 0,\n            'successful': 0,\n            'failed': 0,\n            'skipped': 0,\n        }\n    \n    def _setup_logging(self):\n        \"\"\"Setup logging configuration.\"\"\"\n        log_dir = self.path_manager.get_data_dir() / 'logs'\n        log_dir.mkdir(parents=True, exist_ok=True)\n        \n        log_file = log_dir / 'hybrid_downloader.log'\n        \n        # Configure root logger\n        root_logger = logging.getLogger()\n        root_logger.setLevel(logging.DEBUG)\n        \n        # File handler\n        file_handler = logging.FileHandler(log_file)\n        file_handler.setLevel(logging.DEBUG)\n        \n        # Console handler\n        console_handler = logging.StreamHandler()\n        console_handler.setLevel(logging.INFO)\n        \n        # Formatter\n        formatter = logging.Formatter(\n            '%(asctime)s - %(name)s - %(levelname)s - %(message)s'\n        )\n        file_handler.setFormatter(formatter)\n        console_handler.setFormatter(formatter)\n        \n        root_logger.addHandler(file_handler)\n        root_logger.addHandler(console_handler)\n    \n    def download_track(self, query: str) -> bool:\n        \"\"\"Download a single track.\n        \n        Args:\n            query: Spotify URL, URI, or search query\n            \n        Returns:\n            True if successful, False otherwise\n        \"\"\"\n        self.stats['total'] += 1\n        \n        try:\n            # Get track metadata\n            track = self._get_track_from_query(query)\n            if not track:\n                self.stats['failed'] += 1\n                logger.error(f'Could not find track for query: {query}')\n                return False\n            \n            # Download audio\n            audio_path = self.downloader.download(track)\n            if not audio_path:\n                self.stats['failed'] += 1\n                logger.error(f'Failed to download audio for: {track}')\n                return False\n            \n            # Apply metadata\n            if self.add_cover_art:\n                MetadataHandler.apply_metadata(audio_path, track, add_cover_art=True)\n            \n            self.stats['successful'] += 1\n            logger.info(f'Successfully downloaded: {track}')\n            return True\n            \n        except DownloadError as e:\n            self.stats['failed'] += 1\n            logger.error(f'Download failed: {e}')\n            return False\n        except Exception as e:\n            self.stats['failed'] += 1\n            logger.error(f'Unexpected error: {e}')\n            return False\n    \n    def download_album(self, url: str) -> int:\n        \"\"\"Download all tracks from an album.\n        \n        Args:\n            url: Spotify album URL or URI\n            \n        Returns:\n            Number of successfully downloaded tracks\n        \"\"\"\n        logger.info(f'Downloading album: {url}')\n        \n        try:\n            tracks = self.spotify.get_album_tracks(url)\n            if not tracks:\n                logger.error(f'No tracks found in album: {url}')\n                return 0\n            \n            logger.info(f'Found {len(tracks)} tracks in album')\n            successful = 0\n            \n            for track in tracks:\n                self.stats['total'] += 1\n                try:\n                    audio_path = self.downloader.download(track)\n                    if audio_path:\n                        if self.add_cover_art:\n                            MetadataHandler.apply_metadata(audio_path, track, add_cover_art=True)\n                        self.stats['successful'] += 1\n                        successful += 1\n                    else:\n                        self.stats['failed'] += 1\n                except Exception as e:\n                    self.stats['failed'] += 1\n                    logger.error(f'Failed to download {track}: {e}')\n            \n            return successful\n            \n        except Exception as e:\n            logger.error(f'Failed to download album: {e}')\n            return 0\n    \n    def download_playlist(self, url: str) -> int:\n        \"\"\"Download all tracks from a playlist.\n        \n        Args:\n            url: Spotify playlist URL or URI\n            \n        Returns:\n            Number of successfully downloaded tracks\n        \"\"\"\n        logger.info(f'Downloading playlist: {url}')\n        \n        try:\n            tracks = self.spotify.get_playlist_tracks(url)\n            if not tracks:\n                logger.error(f'No tracks found in playlist: {url}')\n                return 0\n            \n            logger.info(f'Found {len(tracks)} tracks in playlist')\n            successful = 0\n            \n            for track in tracks:\n                self.stats['total'] += 1\n                try:\n                    audio_path = self.downloader.download(track)\n                    if audio_path:\n                        if self.add_cover_art:\n                            MetadataHandler.apply_metadata(audio_path, track, add_cover_art=True)\n                        self.stats['successful'] += 1\n                        successful += 1\n                    else:\n                        self.stats['failed'] += 1\n                except Exception as e:\n                    self.stats['failed'] += 1\n                    logger.error(f'Failed to download {track}: {e}')\n            \n            return successful\n            \n        except Exception as e:\n            logger.error(f'Failed to download playlist: {e}')\n            return 0\n    \n    def download_artist_top_tracks(self, url: str) -> int:\n        \"\"\"Download top 10 tracks from an artist.\n        \n        Args:\n            url: Spotify artist URL or URI\n            \n        Returns:\n            Number of successfully downloaded tracks\n        \"\"\"\n        logger.info(f'Downloading top tracks from artist: {url}')\n        \n        try:\n            tracks = self.spotify.get_artist_top_tracks(url)\n            if not tracks:\n                logger.error(f'No tracks found for artist: {url}')\n                return 0\n            \n            logger.info(f'Found {len(tracks)} top tracks')\n            successful = 0\n            \n            for track in tracks:\n                self.stats['total'] += 1\n                try:\n                    audio_path = self.downloader.download(track)\n                    if audio_path:\n                        if self.add_cover_art:\n                            MetadataHandler.apply_metadata(audio_path, track, add_cover_art=True)\n                        self.stats['successful'] += 1\n                        successful += 1\n                    else:\n                        self.stats['failed'] += 1\n                except Exception as e:\n                    self.stats['failed'] += 1\n                    logger.error(f'Failed to download {track}: {e}')\n            \n            return successful\n            \n        except Exception as e:\n            logger.error(f'Failed to download artist tracks: {e}')\n            return 0\n    \n    def download(self, query: str) -> int:\n        \"\"\"Download from a query (auto-detect type).\n        \n        Args:\n            query: Spotify URL, URI, or search query\n            \n        Returns:\n            Number of successfully downloaded tracks (1 for single track)\n        \"\"\"\n        # Check if it's a Spotify URL/URI\n        if is_valid_spotify_url(query):\n            if '/track/' in query or 'spotify:track:' in query:\n                return 1 if self.download_track(query) else 0\n            elif '/album/' in query or 'spotify:album:' in query:\n                return self.download_album(query)\n            elif '/playlist/' in query or 'spotify:playlist:' in query:\n                return self.download_playlist(query)\n            elif '/artist/' in query or 'spotify:artist:' in query:\n                return self.download_artist_top_tracks(query)\n            else:\n                logger.error(f'Unrecognized Spotify URL: {query}')\n                return 0\n        else:\n            # Treat as search query\n            return 1 if self.download_track(query) else 0\n    \n    def _get_track_from_query(self, query: str) -> Optional[Track]:\n        \"\"\"Get track metadata from URL or search query.\n        \n        Args:\n            query: Spotify URL, URI, or search query\n            \n        Returns:\n            Track object or None\n        \"\"\"\n        try:\n            if is_valid_spotify_url(query):\n                return self.spotify.get_track_by_url(query)\n            else:\n                return self.spotify.search_track(query)\n        except InvalidURLError:\n            logger.error(f'Invalid Spotify URL: {query}')\n            return None\n        except Exception as e:\n            logger.error(f'Error getting track: {e}')\n            return None\n    \n    def get_stats(self) -> dict:\n        \"\"\"Get download statistics.\n        \n        Returns:\n            Dictionary with download stats\n        \"\"\"\n        return self.stats.copy()\n    \n    def get_download_dir(self) -> Path:\n        \"\"\"Get download directory path.\n        \n        Returns:\n            Path to download directory\n        \"\"\"\n        return self.path_manager.get_download_dir()\n    \n    def cleanup(self):\n        \"\"\"Clean up temporary files.\"\"\"\n        try:\n            self.path_manager.cleanup_temp()\n            logger.info('Cleanup completed')\n        except Exception as e:\n            logger.warning(f'Cleanup failed: {e}')\n